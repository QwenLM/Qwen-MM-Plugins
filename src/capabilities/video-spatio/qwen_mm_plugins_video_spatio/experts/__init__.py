"""Spatial-reasoning expert implementations the stateless MCP tools in ``apis/`` delegate to.

Ported from the standalone spatial-agent research code. Internal to this capability — these are
not tool modules and are never registered with ``build_registry``.
"""
